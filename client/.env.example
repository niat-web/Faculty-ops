# Base URL of the FacultyOps API.
# DEV: leave blank — the Vite dev server proxies /api → http://localhost:4000.
# PROD (Vercel): set to your Northflank backend URL, e.g. https://api-xxxx.northflank.app
VITE_API_URL=
